---
name: MATLAB
glyph: M
brand: "#E16737"
order: 60
desc: 一切皆矩阵：创建与逻辑索引、点号运算、函数句柄与闭包、可视化与句柄、table 与数据 IO、反斜杠与分解、向量化与 profiler、工具箱地图——每篇讲透「数学式直译、默认值的来历、性能的手法」。
---

板块元信息目录。

- 本文件：板块名称、描述、品牌色（brand）、排序（order）、手绘兜底字符（glyph）
- logo.svg / logo.png：板块 logo，缺失时按 brand + glyph 手绘兜底
- 本目录下的 .md 文件即为文章；`_` 开头的文件不会被收录
